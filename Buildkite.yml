# .buildkite/pipeline.yml
steps:
  - label: "Detect Clowns"
    # command:
    #   - "bk run"
    plugins:
      - detect-clowns#v2.0.0: ~

  - label: ":shell: Shellcheck"
    plugins:
      - shellcheck#v1.3.0:
          files: hooks/**
      
